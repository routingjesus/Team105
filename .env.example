# Base URL of the Team105 generation API (FastAPI backend, SPEC-001/SPEC-002).
# Inlined into the client bundle at `next build`; must be reachable from the browser.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
