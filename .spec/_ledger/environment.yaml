entries:
  - type: constraint_found
    summary: "Bootcamp Windows hosts need uv via winget and system TLS certs"
    context: >
      Team105 bootcamp Windows machines ship no Python toolchain — python/py
      resolve to the Microsoft Store stub. Install uv with
      `winget install astral-sh.uv`, then create the venv with
      `uv venv --python 3.12`. Corporate TLS interception breaks uv's
      downloads (invalid peer certificate: UnknownIssuer) unless
      UV_SYSTEM_CERTS=true is set (UV_NATIVE_TLS is deprecated). Applies to
      any future Python spec executed on these hosts.
    tags: [environment, windows, uv, tls, python]
    date: 2026-07-30
    source_spec: SPEC-001
  - type: constraint_found
    summary: "Bootcamp Windows hosts also lack a Node toolchain; install a user-local Node zip, not the winget MSI"
    context: >
      The JS counterpart to the uv finding above: node/npm/pnpm are all absent
      on the Team105 bootcamp Windows hosts. `winget install OpenJS.NodeJS.LTS`
      downloads fine but its MSI fails with exit code 1603 because it needs
      interactive administrator elevation, which a non-interactive agent shell
      cannot grant. The working, admin-free path: download the official
      node-vXX-win-x64.zip with `Invoke-WebRequest` (PowerShell's schannel uses
      the system certificate store, so it tolerates the same corporate TLS
      interception that breaks uv/cargo), `Expand-Archive` it into a user dir
      (e.g. C:\Users\<user>\tools\), then prepend that dir to `$env:Path` for
      the session. npm then reaches the registry normally. npm's allow-scripts
      also blocks unrs-resolver's postinstall (a native binding used only by
      eslint-plugin-import's resolver) — harmless for build/test. Applies to any
      future Node/frontend spec on these hosts.
    tags: [environment, windows, node, npm, tls]
    date: 2026-08-04
    source_spec: SPEC-003
  - type: constraint_found
    summary: "Running the full stack locally on bootcamp Windows: stale PATH, held port 8000, PS execution policy"
    context: >
      Walking a user through a local two-terminal run (FastAPI + Next.js)
      surfaced reusable Windows gotchas: (1) winget installs a tool but, without
      Developer Mode/admin, creates no shim in %LOCALAPPDATA%\Microsoft\WinGet\
      Links, so uv/npm aren't on PATH — persist the real tool/package dir to the
      USER PATH via [Environment]::SetEnvironmentVariable('Path',...,'User'), not
      setx (which truncates long PATHs). (2) Cursor's integrated terminal
      inherits the editor's environment from launch time, so a PATH change needs
      a full editor restart or a per-session `$env:Path` prepend — a new terminal
      tab alone won't see it. (3) Port 8000 was held/reserved (bind failed with
      WinError 10013 'access forbidden' and 10048 'in use') even though nothing
      showed as LISTENing — probe bindable ports, run uvicorn with --port 8080,
      and set the frontend's NEXT_PUBLIC_API_BASE_URL to match. (4) PowerShell
      execution policy blocks npm.ps1 ('running scripts is disabled') — use
      `npm.cmd` or set CurrentUser RemoteSigned. (5) Running the API via
      `.venv\Scripts\python.exe -m uvicorn` avoids needing uv on PATH at all.
    tags: [environment, windows, path, ports, powershell]
    date: 2026-08-04
    source_spec: SPEC-003
  - type: constraint_found
    summary: "Windows port probing for a Node/Next server must be dual-stack, or an IPv6 (::) listener is missed and `next start` dies with EADDRINUSE :::<port>"
    context: >
      Sharpens the SPEC-003 "probe bindable ports" note with a specific gotcha
      from the SPEC-004 launcher. A probe that binds only IPv4 loopback
      ([TcpListener]::new([IPAddress]::Loopback, port)) reports a port as free
      even when a Node/next server is already bound to :: (IPv6). Node binds
      dual-stack (IPv6 + IPv4-mapped), so it then fails with EADDRINUSE
      :::<port> and exits. Probe dual-stack instead: bind
      [TcpListener]::new([IPAddress]::IPv6Any, port) with Server.DualMode=$true,
      and also consult `Get-NetTCPConnection -LocalPort <p> -State Listen`, which
      sees listeners on any address family and catches reserved-but-not-listening
      ports (the WinError 10013 port-8000 case). Any Windows launcher that
      auto-selects a port for a Node/Next server needs this.
    tags: [environment, windows, ports, ipv6, powershell]
    date: 2026-08-04
    source_spec: SPEC-004
  - type: pattern_discovered
    summary: "Embeddable CPython ._pth ignores CWD and PYTHONPATH; add a relative path to the app root so imports resolve"
    context: >
      Windows embeddable Python initializes sys.path only from python*._pth.
      The current working directory is not on sys.path, and PYTHONPATH is
      ignored while ._pth is present. A portable launcher that runs
      `uvicorn backend.main:app` from the bundle's app/ directory will fail
      `import backend.main` unless packaging adds a path such as `..\..\app`
      (relative to runtime/python/) to the ._pth file. Regular CPython
      venvs (e.g. run-local's .venv) do not need this.
    tags: [environment, windows, python, embeddable, portable]
    date: 2026-08-06
    source_spec: SPEC-018
  - type: pitfall
    summary: "PowerShell packaging helpers must isolate return values from npm stdout and avoid unquoted robocopy on paths with spaces or !"
    context: >
      A function that runs `npm run build` and then `return $path` captures
      npm's stdout as part of the function output in PowerShell 5.1 — the
      caller receives build logs instead of the path. Pipe npm to Out-Host
      (or otherwise discard success-stream noise) before returning a single
      path. Separately, Start-Process robocopy with unquoted ArgumentList
      splits paths like `C:\Users\...\ ! Cursor\...` on spaces; prefer
      Copy-Item -LiteralPath tree copies or carefully quoted args.
    tags: [environment, windows, powershell, packaging]
    date: 2026-08-06
    source_spec: SPEC-018
