schema_version: "2026.04"
status: done
created: 2026-08-04
updated: 2026-08-05

# Origin (optional — informational, not a dependency)
# source: "https://..."                 # Shareable origin breadcrumb, never a local filesystem path like /home/... or /Users/...
# tracking_id: ""                       # External reference (Notion task, Jira ticket, etc.)

# Dependencies (optional — advisory ordering, not execution blockers)
# depends_on:
#   - "SPEC-030"                        # Lightweight sequencing; upstream should usually be done first
# blocked_by:
#   - spec: "SPEC-030"
#     until_status: "ready"            # required; ready | done

# Coordination (optional — blast radius classification for parallel work)
# coordination:
#   blast_radius: ""                   # structural | isolated | mixed
#   parallel_safe:                     # true only when blast_radius is isolated; do not set independently

# Branching (optional — non-default branch topology for stacked or integration workflows)
# branching:
#   base_branch: ""                    # Branch to fork from; defaults to repo default branch when absent
#   target_branch: ""                  # PR base branch; defaults to repo default branch when absent

# Freshness snapshot (populate when status reaches ready)
# ready_sha: ""                         # Full git HEAD commit recorded at the ready transition; used for advisory staleness checks

# Completion (populate when status reaches done)
# completion:
#   date: 2026-08-04
#   acceptance_criteria_waived:         # Omit if none waived
#     - acceptance_criteria: ""
#       rationale: ""
#   self_review_waived:                 # Omit if none waived; pr-review blocking findings the user accepted to proceed
#     - finding: ""
#       rationale: ""
#   pull_requests:
#     - "https://github.com/..."

# Learnings (add during and after execution)
# learnings:
#   - type: pattern_discovered          # See references/spec-format.md for all types
#     summary: ""
#     context: ""
#     tags: []
#     date: 2026-08-04
ready_sha: "14f8bf095e0f481c2d2af2f0d0792452055d63dd"

coordination:
  blast_radius: isolated
  parallel_safe: true

completion:
  date: 2026-08-05
  pull_requests: []

learnings:
  - type: pattern_discovered
    summary: >-
      Reported bug did not reproduce: achievable_frequency_values/build_rows
      already select fractional Frequency values correctly, verified at the
      generator, full .xlsx round-trip, and HTTP API (TestClient) layers.
    context: >-
      Traced backend/generators/stop.py, backend/schemas/stop_config.py,
      lib/build-config.ts, and components/wizard/stop-questions.tsx for
      SPEC-006's "Frequency collapses to 1" report; every layer produced
      correct fractional (0.5) output for multiple weeks/seed/stop_count
      combinations.
    tags: ["bug-not-reproduced", "stop-generator", "frequency"]
    date: 2026-08-05
  - type: gap_found
    summary: >-
      Existing test_frequency_values_come_from_requested_subset only
      asserted a subset relationship (seen <= requested), which passes even
      if a fractional value never actually appears in output — added two
      tests that assert 0.5 explicitly appears, one against the in-memory
      row list and one against the round-tripped .xlsx bytes.
    context: "tests/test_stop_generator.py"
    tags: ["test-coverage", "regression-test"]
    date: 2026-08-05
